# Dependencies
node_modules/

# Secrets — never commit these
.env
.env.*
!.env.example
.vercel

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Drive import working files — holds file ids for unlisted galleries,
# including client shoots. Local only; the repo is public.
.drive-import/
